@page
@model checkpoint_web.Pages.Admin.Tareas.DeleteModel
@{
    ViewData["Title"] = "Eliminar Tarea";
    Layout = Request.Headers["X-Requested-With"] == "XMLHttpRequest" ? null : "/Pages/Shared/_AdminLayout.cshtml";
}

<h2>Eliminar Tarea</h2>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
}

<div class="alert alert-danger">
    <h5>Â¿EstÃ¡ seguro que desea eliminar esta tarea?</h5>
    <p>Esta acciÃ³n no se puede deshacer.</p>
</div>

<div class="card">
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">TÃ­tulo</dt>
            <dd class="col-sm-9">@Model.Tarea.Titulo</dd>

            <dt class="col-sm-3">DescripciÃ³n</dt>
            <dd class="col-sm-9">@Model.Tarea.Descripcion</dd>

            <dt class="col-sm-3">Estado</dt>
            <dd class="col-sm-9">@Model.Tarea.Estado</dd>

            <dt class="col-sm-3">Prioridad</dt>
            <dd class="col-sm-9">@Model.Tarea.Prioridad</dd>
        </dl>

        <form method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Tarea.Id" />
            <div class="d-flex justify-content-between">
                <a asp-page="Index" class="btn btn-secondary">Cancelar</a>
                <button type="submit" class="btn btn-danger">Confirmar EliminaciÃ³n</button>
            </div>
        </form>
    </div>
</div>
